<%= form_with(model: puesto, local: true) do |form| %>
  <% if puesto.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(puesto.errors.count, "error") %> prohibited this puesto from being saved:</h2>

      <ul>
      <% puesto.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nombre %>
    <%= form.text_field :nombre %>
  </div>

  <div class="field">
    <%= form.label :rotulo %>
    <%= form.text_field :rotulo %>
  </div>

  <div class="field">
    <%= form.label :fila_id %>
    <%= form.collection_select(:fila_id, Fila.all, :id, :nombre, include_blank: true) %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
